[project]
name = "ai-projects"
version = "0.1.0"
description = "Ai projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["python-dotenv>=1.1.1","openai>=1.109.1"]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
